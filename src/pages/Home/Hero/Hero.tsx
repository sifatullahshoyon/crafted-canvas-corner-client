import HeroCarousel from "@/components/Carousel/HeroCarousel";

const Hero = () => {
  return (
    <div className="mt-[200px] md:mt-24 mx-auto">
      <HeroCarousel />
    </div>
  );
};

export default Hero;
